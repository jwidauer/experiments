project('source_location', 'cpp',
  version : '0.1',
  default_options : ['warning_level=everything',
                     'cpp_std=c++23'])

exe = executable('source_loc', 'src/source_loc.cpp',
  install : true)

test('basic', exe)
