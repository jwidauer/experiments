project('from_bits', 'cpp',
  version : '0.1',
  default_options : ['warning_level=3',
                     'cpp_std=c++20'])

exe = executable('from_bits', 'from_bits.cpp',
  install : true)

test('basic', exe)
