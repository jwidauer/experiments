project('signal_jumping', 'cpp',
  version : '0.1',
  default_options : ['warning_level=3',
                     'cpp_std=c++23'])

exe = executable('signal_jumping', 'signal_jumping.cpp',
  install : true)

test('basic', exe)
